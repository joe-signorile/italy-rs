// Provides stb_image's implementation for environment_cdf_test, which links environment.cpp (declares but doesn't define it) without linking gltf_loader.cpp.
#define STB_IMAGE_IMPLEMENTATION
#include <stb_image.h>
